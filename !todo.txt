- disable f***ing tty cursor 
	do not working on VM Box!
- take a look for long ship names in info_box
+ optimize drawmap->drawpoly->draw_outline, to not draw hidden ribs
+ add in-font icon for pos and satellites
+ touch driver - two types of points = rect and circle
+ touch driver - add pressure param
+ add large font for info window
+ update video_driver::draw_text -> for black pixels
- check OSM maps for extract inland water ways
+ make font for infoline with monospaced numbers ==>> https://github.com/Tecate/bitmap-fonts, https://www.dafont.com/bitmap.php
- calculate heading from 2 last GPS positions
+ remove trailing zeroes from range numbers
+ show own position in deg/min/sec
- check dms symbols on small screen
+ make NMEA_LIST a fifo instead of vector
+ check video_driver::circle for exceeds Y-axis (make cicrcle cache)
- check for `socket disconnected`
+ gps tracker session ID берется при старте каждый тик gps мы пишем со временем и номером сессии возможно сразу считаем расстояние для последних непросчитанных шагов в сессии, это может быть как 1 шаг ( так и вся сессия - это наверно лучше проверять при старте, и сразу считать если сессия не закрыта)
+ смотрим есть ли у нас уже такая строка в gps_total с session = current если нет - вставляем новую строку (`session`,`points`,`dist`) = ( current, 1, 0.0) и выходим 	- если строка уже есть, 		берем две последних записи		gps_total.dist += считаем разницу по ним		gps_total.points++

- добавить TouchZone для списка кораблей
- добавить инфо окно для просмотра инфы о корабле по клику
- запустить один кораблик и посмотреть что происходит с углом

- проверить как создается контур кораблика, в частности градусы

   // offset MYPOS from gps center to geometrical boat center

+ сделать в шрифте цифры monospaced

+ упорядочить инфо строку, данные для показа	время (utc? local?)		время восхода				координаты								спутников видно/используем	дата					время захода				коорд ч.2								пройд путь
- посмотреть есть ли возможность показа временной зоны

+ привести в порядок (чтобы на входе были норм радианы) FloatPoint FloatPoint::latlon2meter() 